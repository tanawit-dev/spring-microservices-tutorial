spring:  application:    name: config-server
  profiles:    active:    - native
  cloud:    config:      server:        native:          search-locations:          - file:///${user.name}/application-config
          
eureka:  client:    region: default